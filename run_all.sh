# Run all the sentences from gold, store the results in pred_dir, evaluate the results and store the scores in scores.json, results in result.tsv and errors in error.tsv files. 

# Usage:
python3 run_all.py gold_dir/ pred_dir/ result.tsv error.tsv scores.json
